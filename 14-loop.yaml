- name: install packages
  hosts: web
  become: yes
  tasks:
    name: installin the package
    ansible.builtin.package:
      name: {{items}}
      state: present
    loop:
    - mysql
    - nginx
    - postfix